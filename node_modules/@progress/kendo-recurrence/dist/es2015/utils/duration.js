// TODO: ZonedDate.prototype.subtract(other: ZonedDate): number
/**
 * Returns a duration between start and end dates.
 *
 * @hidden
 */
export const duration = (start, end) => {
    return end.getTime() - start.getTime();
};
