"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var utils_1 = require("./utils");
var normalizeWeekDay = function (day, weekStart) { return (day + (day < weekStart ? 7 : 0)); };
var weekDaysSortPredicate = function (weekStart) { return function (a, b) { return (normalizeWeekDay(a.day, weekStart) - normalizeWeekDay(b.day, weekStart)); }; };
var sortWeekDays = function (rrule) {
    var weekDays = rrule.weekDays, weekStart = rrule.weekStart;
    if (!weekDays) {
        return;
    }
    weekDays.sort(weekDaysSortPredicate(weekStart));
};
/**
 * @hidden
 *
 * Internal method for parsing of iCal recurrence rule string.
 */
exports.parseRule = function (options) {
    if (!options) {
        return null;
    }
    var recurrenceRule = options.recurrenceRule, _a = options.weekStart, weekStart = _a === void 0 ? 0 : _a;
    var rrule = {};
    var splits, value;
    var idx = 0, length;
    var hasModifiers = false;
    var rule, part, parts;
    var property;
    if (!recurrenceRule) {
        return null;
    }
    parts = recurrenceRule.split("\n");
    if (!parts[1] && (recurrenceRule.indexOf("DTSTART") !== -1 ||
        recurrenceRule.indexOf("DTEND") !== -1 ||
        recurrenceRule.indexOf("EXDATE") !== -1)) {
        parts = recurrenceRule.split(" ");
    }
    for (idx = 0, length = parts.length; idx < length; idx++) {
        part = (parts[idx] || '').trim();
        if (part.indexOf("DTSTART") !== -1) {
            rrule.start = utils_1.parseDateTime(part);
        }
        else if (part.indexOf("DTEND") !== -1) {
            rrule.end = utils_1.parseDateTime(part);
        }
        else if (part.indexOf("EXDATE") !== -1) {
            rrule.exceptionDates = utils_1.parseDateTimeList(part);
        }
        else if (part.indexOf("RRULE") !== -1) {
            rule = part.substring(6);
        }
        else if (part.trim()) {
            rule = part;
        }
    }
    rule = rule.split(";");
    for (idx = 0, length = rule.length; idx < length; idx++) {
        property = rule[idx];
        splits = property.split("=");
        value = (splits[1] || '').trim().split(",");
        switch (splits[0].trim().toUpperCase()) {
            case "FREQ":
                rrule.freq = value[0].toLowerCase();
                break;
            case "UNTIL":
                rrule.until = utils_1.parseISODate(value[0]);
                break;
            case "COUNT":
                rrule.count = parseInt(value[0], 10);
                break;
            case "INTERVAL":
                rrule.interval = parseInt(value[0], 10);
                break;
            case "BYSECOND":
                rrule.seconds = utils_1.parseArray(value, { start: 0, end: 60 });
                hasModifiers = true;
                break;
            case "BYMINUTE":
                rrule.minutes = utils_1.parseArray(value, { start: 0, end: 59 });
                hasModifiers = true;
                break;
            case "BYHOUR":
                rrule.hours = utils_1.parseArray(value, { start: 0, end: 23 });
                hasModifiers = true;
                break;
            case "BYMONTHDAY":
                rrule.monthDays = utils_1.parseArray(value, { start: -31, end: 31 });
                hasModifiers = true;
                break;
            case "BYYEARDAY":
                rrule.yearDays = utils_1.parseArray(value, { start: -366, end: 366 });
                hasModifiers = true;
                break;
            case "BYMONTH":
                rrule.months = utils_1.parseArray(value, { start: 1, end: 12 });
                hasModifiers = true;
                break;
            case "BYDAY":
                rrule.weekDays = utils_1.parseWeekDayList(value);
                hasModifiers = true;
                break;
            case "BYWEEKNO":
                rrule.weeks = utils_1.parseArray(value, { start: -53, end: 53 });
                hasModifiers = true;
                break;
            case "BYSETPOS":
                rrule.positions = utils_1.parseArray(value, { start: -366, end: 366 });
                break;
            case "WKST":
                rrule.weekStart = utils_1.parseWeekDay(value[0]);
                break;
            default:
                break;
        }
    }
    if (rrule.freq === undefined || (rrule.count !== undefined && rrule.until)) {
        return null;
    }
    if (rrule.weekStart === undefined) {
        rrule.weekStart = weekStart;
    }
    if (!rrule.interval) {
        rrule.interval = 1;
    }
    if (rrule.positions && !hasModifiers) {
        rrule.positions = [];
    }
    sortWeekDays(rrule);
    return rrule;
};
