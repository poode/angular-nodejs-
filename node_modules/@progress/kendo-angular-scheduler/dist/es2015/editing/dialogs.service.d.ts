import { ViewContainerRef, ChangeDetectorRef } from '@angular/core';
import { DialogService } from '@progress/kendo-angular-dialog';
import { Observable } from 'rxjs/Observable';
import { EditMode, CrudOperation } from '../types';
import { LocalizationService } from '@progress/kendo-angular-l10n';
/**
 * @hidden
 */
export interface RecurringDialogCloseResult {
    text: string;
    value: EditMode;
}
/**
 * @hidden
 */
export interface ConfirmationDialogCloseResult {
    text: string;
    value: boolean;
}
/**
 * @hidden
 */
export declare class DialogsService {
    private dialogService;
    private localization;
    private changeDetector;
    container: ViewContainerRef;
    constructor(dialogService: DialogService, localization: LocalizationService, changeDetector: ChangeDetectorRef);
    openRemoveConfirmationDialog(): Observable<boolean>;
    openRecurringConfirmationDialog(operation: CrudOperation): Observable<EditMode>;
    textFor(key: string): string;
}
