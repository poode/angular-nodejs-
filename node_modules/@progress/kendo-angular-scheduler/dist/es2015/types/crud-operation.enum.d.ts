/**
 * The selected CRUD operation when the user edits or removes recurring events.
 *
 * The supported values are:
 * * `Edit`
 * * `Remove`
 */
export declare const enum CrudOperation {
    Edit = 0,
    Remove = 1,
}
