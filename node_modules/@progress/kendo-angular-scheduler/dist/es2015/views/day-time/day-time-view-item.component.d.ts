import { ElementRef, Renderer2 } from '@angular/core';
import { BaseViewItem } from '../view-items/base-view-item';
import { DayTimeSlotService } from '../day-time/day-time-slot.service';
import { IntlService } from '@progress/kendo-angular-intl';
import { LocalizationService } from '@progress/kendo-angular-l10n';
/**
 * @hidden
 */
export declare class DayTimeViewItemComponent extends BaseViewItem {
    private intlService;
    vertical: boolean;
    isAllDay: boolean;
    readonly eventTitle: string;
    readonly eventTime: string;
    constructor(intlService: IntlService, slotService: DayTimeSlotService, localization: LocalizationService, element: ElementRef, renderer: Renderer2);
}
